package com.example.server.model;

/**
 * Work specialty assigned when an admin creates a {@link UserRole#TECHNICIAN} account.
 */
public enum TechnicianCategory {
    ELECTRICAL,
    NETWORK,
    IT_SUPPORT,
    FACILITIES,
    MAINTENANCE,
    GENERAL
}
