import React from 'react';

// This page is optional and not used by the router anymore, but
// it's included only so that the Tailwind IntelliSense warnings
// about `flex-grow` disappear if VS Code still has it open.
const Test = () => {
  return (
    <main className="grow">
      {/* placeholder */}
    </main>
  );
};

export default Test;
